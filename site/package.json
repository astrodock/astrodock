{
  "name": "@astrodock/site",
  "version": "0.0.16",
  "private": true,
  "description": "The astrodock.ai marketing site. Static HTML — the build is a copy into dist/.",
  "scripts": {
    "build": "node build.mjs"
  }
}
