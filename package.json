{
  "name": "toolstead",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hostable, AI-native app platform: deploy, auth, host, route and monitor small apps on your own box.",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @toolstead/control-plane",
    "dev:admin": "npm run dev -w @toolstead/admin",
    "build": "npm run build -w @toolstead/admin",
    "migrate": "npm run migrate -w @toolstead/control-plane",
    "seed": "npm run seed -w @toolstead/control-plane",
    "test": "npm test --workspaces --if-present",
    "cli": "node packages/cli/bin/toolstead.js"
  },
  "engines": {
    "node": ">=20"
  }
}
